{
  "schemaVersion": 1,
  "id": "mca_rcc",
  "version": "${version}",
  "name": "mca-rcc",
  "description": "Client-side HTTP JSON-RPC bridge exposing the local Minecraft player to an external agent. Input-synthesis + world reads only; no pathfinding or hack modules.",
  "environment": "client",
  "entrypoints": { "client": ["com.btone.c.BtoneC"] },
  "mixins": ["btone-mod-c.mixins.json"],
  "depends": {
    "fabricloader": ">=0.16.0",
    "minecraft": "~26.2",
    "java": ">=25",
    "fabric-api": "*"
  }
}
