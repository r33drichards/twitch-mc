return {
  x = api:x(), y = api:y(), z = api:z(),
  yaw = api:yaw(), pitch = api:pitch(),
  health = api:health(), food = api:food(),
  held = api:heldItem(), slot = api:hotbarSlot(),
  dim = api:dim(), name = api:name(),
}
